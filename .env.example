# Caminho para o CSV carregado pelo utilizador
CSV_PATH=backend/data/uploaded.csv

# Diretório onde o Qdrant vai guardar os dados (persistência do índice vetorial)
PERSIST_DIR=storage/qdrant

# Nome da coleção no Qdrant
COLLECTION_NAME=dataset

# Nome do modelo LLM a usar no Ollama (deve coincidir com 'ollama list')
LLM_MODEL=llama3:instruct

# Nome do modelo de embeddings a usar no Ollama
EMBEDDING_MODEL=nomic-embed-text

# Modo de operação do agente (ex: tools, auto, etc.)
AGENT_LLM_MODE=tools
